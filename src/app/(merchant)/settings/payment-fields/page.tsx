import type { Metadata } from "next";
import { PlaceholderPage } from "@/components/placeholder-page";

export const metadata: Metadata = { title: "Payment fields" };

export default function PaymentFieldsPage() {
  return <PlaceholderPage category="Settings" title="Payment fields" />;
}
